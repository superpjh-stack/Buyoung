/**
 * NOTE: The "/" route is actually served by app/page.tsx.
 * This file exists because create-next-app created a route group structure,
 * but the root page is intentionally handled outside the group.
 * This component acts as a fallback that should never render.
 */
export { default } from "@/app/page";
